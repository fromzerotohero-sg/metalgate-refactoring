"use client";
import { useEffect, useState } from "react";
import { api, ApiError } from "@/src/lib/api";
export default function VerifyEmailPage(){const [message,setMessage]=useState("Verifica in corso… · Verifying… · Verificando…");useEffect(()=>{const token=new URLSearchParams(location.search).get("token");if(!token){setMessage("Token di verifica mancante. · Verification token is missing. · Falta el token de verificación.");return;}api.verifyEmail(token).then(()=>{setMessage("Email verificata. Ti portiamo al tuo account… · Email verified. Taking you to your account… · Email verificado. Te llevamos a tu cuenta…");setTimeout(()=>location.assign("/account"),800);}).catch((e:ApiError)=>setMessage(`${e.message} · Verification failed. · Verificación fallida.`));},[]);return <main className="form-page"><section className="form-card"><img className="form-logo" src="/logo.webp" alt="From Zero To Hero"/><h1>{message}</h1></section></main>}
