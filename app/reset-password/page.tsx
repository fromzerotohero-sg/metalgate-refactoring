"use client";
import { FormEvent, useState } from "react";
import { api, ApiError } from "@/src/lib/api";
export default function ResetPasswordPage(){const [password,setPassword]=useState("");const [message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();const token=new URLSearchParams(location.search).get("token")??"";if(!token){setMessage("Token di reset mancante.");return;}try{await api.resetPassword({token,password});setMessage("Password aggiornata. Ora puoi accedere.");}catch(error){setMessage((error as ApiError).message);}}return <main className="form-page"><section className="form-card"><img className="form-logo" src="/logo.webp" alt="From Zero To Hero"/><p className="eyebrow">SICUREZZA</p><h1>Nuova password.</h1><form onSubmit={submit}><label>Password nuova<input type="password" minLength={8} required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button primary">Salva password</button></form>{message&&<p>{message}</p>}<a href="/login">Torna al login</a></section></main>}
